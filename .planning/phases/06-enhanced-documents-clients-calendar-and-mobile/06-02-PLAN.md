---
phase: 06-enhanced-documents-clients-calendar-and-mobile
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(tabs)/clients/[id].tsx
  - app/(tabs)/clients/new.tsx
  - app/(tabs)/calendar/event/new.tsx
  - app/(tabs)/calendar/event/[id].tsx
  - app/(tabs)/calendar/index.tsx
  - app/(tabs)/cases/[id].tsx
autonomous: true

must_haves:
  truths:
    - "User can create a new client through a 4-step intake workflow (contact, conflict check, consultation, onboarding)"
    - "User can view communication history on a client detail screen and add new entries"
    - "User can view and add client-level documents (ID, powers of attorney) on client detail"
    - "Corporate client detail shows contacts with primary contact indicator and ability to add contacts"
    - "User is warned about scheduling conflicts when creating or editing calendar events"
    - "User can edit existing calendar events from the event detail screen"
    - "User can set recurrence pattern (daily, weekly, monthly) when creating or editing events"
    - "User can call a client or navigate to court from case detail screen via quick-action buttons"
  artifacts:
    - path: "app/(tabs)/clients/new.tsx"
      provides: "Multi-step intake workflow screen with 4 steps"
      contains: "IntakeStep"
    - path: "app/(tabs)/clients/[id].tsx"
      provides: "Client detail with communication history, client documents, enhanced contacts"
      contains: "communicationHistory"
    - path: "app/(tabs)/calendar/event/new.tsx"
      provides: "Event creation form with conflict detection and recurrence options"
      contains: "conflict"
    - path: "app/(tabs)/calendar/event/[id].tsx"
      provides: "Event detail with edit mode, recurrence display"
      contains: "editMode"
    - path: "app/(tabs)/cases/[id].tsx"
      provides: "Case detail with quick-action buttons (call client, navigate to court)"
      contains: "Linking.openURL"
  key_links:
    - from: "app/(tabs)/clients/new.tsx"
      to: "src/services/mock/mock-client.ts"
      via: "services.clients.createClient"
      pattern: "services\\.clients\\.createClient"
    - from: "app/(tabs)/clients/[id].tsx"
      to: "src/services/mock/mock-client.ts"
      via: "services.communications.getByClientId + services.clientDocuments.getByClientId"
      pattern: "services\\.communications|services\\.clientDocuments"
    - from: "app/(tabs)/calendar/event/new.tsx"
      to: "src/services/mock/mock-client.ts"
      via: "services.calendarEvents.getConflictingEvents"
      pattern: "getConflictingEvents"
    - from: "app/(tabs)/calendar/event/[id].tsx"
      to: "src/services/mock/mock-client.ts"
      via: "services.calendarEvents.updateEvent"
      pattern: "updateEvent"
---

<objective>
Build client intake workflow, communication history, client documents, calendar enhancements (conflict detection, event editing, recurrence), and mobile quick actions.

Purpose: Complete all remaining Phase 6 requirements (CLNT-01 through CLNT-04, CAL-01 through CAL-03, MOB-02).
Output: Enhanced client, calendar, and case detail screens with all v1.1 Phase 6 functionality.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-enhanced-documents-clients-calendar-and-mobile/06-01-SUMMARY.md
@src/services/types.ts
@src/services/mock/mock-client.ts
@app/(tabs)/clients/[id].tsx
@app/(tabs)/clients/new.tsx
@app/(tabs)/calendar/event/new.tsx
@app/(tabs)/calendar/event/[id].tsx
@app/(tabs)/cases/[id].tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build client intake workflow, communication history, client documents, and corporate contacts</name>
  <files>
    app/(tabs)/clients/new.tsx
    app/(tabs)/clients/[id].tsx
  </files>
  <action>
    **Client intake workflow (app/(tabs)/clients/new.tsx) -- replace existing simple form with multi-step wizard:**

    CLNT-01: 4-step intake workflow with step indicator at top:
    1. **Contact step**: Collect client type (individual/corporate toggle), name fields (firstName/lastName or companyName), email, phone, address, city, JMBG/PIB/MB. This is essentially the existing form but now as step 1.
    2. **Conflict Check step**: Show a simulated conflict check. Display a search against existing clients by name (call `services.clients.searchClients`). Show results if any matches found with a yellow warning banner "Potential conflict detected". If no matches, show green checkmark "No conflicts found". User can proceed regardless.
    3. **Consultation step**: A text area for consultation notes, a date picker for consultation date, and a checkbox "Retainer agreement signed". This data is informational (stored as part of the client creation flow but not persisted separately -- just include consultation notes in the final step summary).
    4. **Onboarding step**: Summary review of all entered data displayed in read-only cards. A "Create Client" button that calls `services.clients.createClient` with the collected data, then navigates back.

    Implementation:
    - Use a `step` state (0-3) and render different content per step.
    - Step indicator: horizontal row of 4 circles connected by lines, filled circle for current/completed steps, golden color for active.
    - Bottom buttons: "Back" (except step 0) and "Next" / "Create" (step 3).
    - Validate required fields before advancing (type, name fields required).
    - Use inline styles, SECTION_CARD style for form sections.

    **Client detail enhancements (app/(tabs)/clients/[id].tsx) -- add 3 new sections below existing content:**

    CLNT-02 - Communication History section:
    - Load via `services.communications.getByClientId(id)`.
    - Display as a timeline list: each entry shows icon (call=call-outline, meeting=people-outline, email=mail-outline, note=document-text-outline), subject, date, and truncated content.
    - "Add" button (golden, small) in section header opens a Modal with: type selector (4 chips), subject TextInput, content TextInput, date (today default). Save calls `services.communications.create(...)`.
    - Empty state: "No communication history yet" with add prompt.

    CLNT-03 - Client Documents section:
    - Load via `services.clientDocuments.getByClientId(id)`.
    - Display as a list of document cards: type badge (id-card, passport, etc.), name, expiry date if set.
    - "Add" button in section header opens DocumentPicker, then a Modal to select document type from CLIENT_DOC_TYPES and optionally set expiry date.
    - Delete via swipe or trash icon button with confirmation Alert.

    CLNT-04 - Corporate Contacts section (only for corporate clients):
    - Enhance existing representatives section: add a primary contact indicator (golden star icon) next to the primary contact.
    - Add an "Add Contact" button that opens a Modal with name, role, phone, email fields. Calls `services.clients.updateClient(id, { representatives: [...existing, newRep] })`.
    - Each contact shows call/email action buttons (already exists -- keep those).

    Use SECTION_CARD style for all new sections. Follow existing card patterns from the screen.
  </action>
  <verify>Run `npx tsc --noEmit`. Navigate to client new screen and verify 4-step wizard flow works (step indicator, form validation, conflict check, summary, create). Navigate to client detail and verify communication history, client documents, and corporate contacts sections appear with data.</verify>
  <done>CLNT-01: Multi-step intake workflow creates clients. CLNT-02: Communication history displays and accepts new entries. CLNT-03: Client documents display with add/delete. CLNT-04: Corporate contacts show primary indicator and accept new contacts.</done>
</task>

<task type="auto">
  <name>Task 2: Build calendar enhancements (conflict detection, event editing, recurrence) and mobile quick actions</name>
  <files>
    app/(tabs)/calendar/event/new.tsx
    app/(tabs)/calendar/event/[id].tsx
    app/(tabs)/calendar/index.tsx
    app/(tabs)/cases/[id].tsx
  </files>
  <action>
    **Calendar event creation -- conflict detection and recurrence (app/(tabs)/calendar/event/new.tsx):**

    CAL-01 - Conflict detection:
    - After user selects date and time, call `services.calendarEvents.getConflictingEvents(date, startTime, endTime)`.
    - If conflicts found, display a yellow warning banner below the time pickers showing: warning icon, "Scheduling conflict detected", and a list of conflicting event titles with their times.
    - User can still proceed (warning, not blocking). The warning updates reactively when date/time changes (use useEffect with debounce).

    CAL-03 - Recurrence pattern:
    - Add a "Recurrence" section below the time pickers (above linked case).
    - Toggle: "No recurrence" (default) vs "Recurring event".
    - When recurring: show type picker chips (Daily, Weekly, Monthly), interval number input (default 1), optional end date picker.
    - For weekly: show day-of-week checkboxes (Mon-Sun), defaulting to the selected date's day.
    - Pass `recurrence` field to `createEvent`.

    **Calendar event detail -- edit mode (app/(tabs)/calendar/event/[id].tsx):**

    CAL-02 - Event editing:
    - Add an "Edit" button in the header (pencil icon, like client detail pattern).
    - When tapped, switch to edit mode: all fields become editable (reuse form elements from new.tsx pattern -- TextInput for title, date picker, time pickers, type selector, location, notes, case picker, recurrence).
    - Add conflict detection in edit mode (same as creation, using `excludeId` to exclude current event).
    - "Save" button calls `services.calendarEvents.updateEvent(id, updatedData)`, exits edit mode.
    - "Cancel" button restores original values, exits edit mode.
    - Add a "Delete" button (red, bottom of screen) with Alert confirmation. Calls `services.calendarEvents.deleteEvent(id)`, navigates back.
    - In view mode, display recurrence info if present (e.g., "Repeats weekly on Monday" or "Repeats daily").

    **Calendar index -- show recurrence badge (app/(tabs)/calendar/index.tsx):**
    - On EventCard component, if `event.recurrence` exists, show a small recurrence icon (repeat-outline) next to the time text.

    **Mobile quick actions (app/(tabs)/cases/[id].tsx):**

    MOB-02 - Quick action buttons on case detail:
    - Add a floating quick-action bar at the bottom of the case detail screen (above the scroll content, fixed position).
    - Two buttons:
      1. "Call Client" (phone icon + label): Uses `Linking.openURL('tel:...')` with the client's phone number. Load client data via `services.clients.getClientById(case.clientId)` to get phone. Disable if no phone.
      2. "Navigate to Court" (navigate icon + label): Uses `Linking.openURL('https://maps.google.com/?q=...')` with the court address. Use `case.court` field. Disable if no court assigned.
    - Style: Two equal-width buttons in a row, navy and golden backgrounds respectively, rounded, with icons. Similar to the document bottom action bar pattern.

    Follow all established patterns: inline styles, IoniconsName, colors, useTranslation, useServices.
  </action>
  <verify>Run `npx tsc --noEmit`. Test event creation: select overlapping date/time and verify conflict warning appears. Create a recurring event and verify recurrence data persists. Open event detail, tap edit, modify fields, save. Delete an event. Check case detail for quick-action buttons at bottom.</verify>
  <done>CAL-01: Conflict warnings display during event creation/editing. CAL-02: Events can be edited and deleted from detail screen. CAL-03: Recurrence pattern (daily/weekly/monthly) can be set on events. MOB-02: Case detail has call-client and navigate-to-court quick actions.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Client intake: 4-step wizard (contact > conflict check > consultation > onboarding) creates a client
- Client detail: shows communication history, client documents, enhanced corporate contacts
- Calendar creation: conflict warning appears for overlapping events, recurrence options available
- Calendar detail: edit mode allows modifying all fields, delete removes event
- Case detail: quick-action buttons for calling client and navigating to court
</verification>

<success_criteria>
1. CLNT-01: Client intake wizard flows through 4 steps and creates client
2. CLNT-02: Communication history displays on client detail with add capability
3. CLNT-03: Client documents section shows documents with add/delete
4. CLNT-04: Corporate contacts show primary indicator and accept new contacts
5. CAL-01: Conflict detection warns about overlapping events
6. CAL-02: Events can be edited from detail screen
7. CAL-03: Recurring events can be created with daily/weekly/monthly patterns
8. MOB-02: Quick-action buttons on case detail for call and navigation
9. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-enhanced-documents-clients-calendar-and-mobile/06-02-SUMMARY.md`
</output>

---
phase: 06-enhanced-documents-clients-calendar-and-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/types.ts
  - src/services/mock/data/documents.ts
  - src/services/mock/data/clients.ts
  - src/services/mock/data/calendar-events.ts
  - src/services/mock/data/communication-history.ts
  - src/services/mock/data/client-documents.ts
  - src/services/mock/mock-client.ts
  - src/i18n/locales/en/documents.json
  - src/i18n/locales/en/clients.json
  - src/i18n/locales/en/calendar.json
  - src/i18n/locales/sr-Latn/documents.json
  - src/i18n/locales/sr-Latn/clients.json
  - src/i18n/locales/sr-Latn/calendar.json
  - src/i18n/locales/sr-Cyrl/documents.json
  - src/i18n/locales/sr-Cyrl/clients.json
  - src/i18n/locales/sr-Cyrl/calendar.json
  - app/(tabs)/cases/documents/[caseId].tsx
  - app/(tabs)/cases/documents/preview/[docId].tsx
autonomous: true

must_haves:
  truths:
    - "Documents display in categorized folders (Pleadings, Evidence, Correspondence, etc.) on the document list screen"
    - "Documents show metadata fields (tags, description) and version history entries"
    - "User can create recurring calendar events with daily, weekly, or monthly patterns"
    - "User can view communication history and client-level documents on client detail screens"
    - "After capturing a document photo, user sees a confirmation prompt before the document is saved"
  artifacts:
    - path: "src/services/types.ts"
      provides: "DocumentFolder, DocumentVersion, RecurrencePattern, CommunicationEntry, ClientDocument types + updated service interfaces"
      contains: "DocumentFolder"
    - path: "src/services/mock/data/documents.ts"
      provides: "Mock documents with folder, tags, description, versions"
      contains: "folderId"
    - path: "src/services/mock/data/communication-history.ts"
      provides: "Mock communication history entries"
      contains: "mockCommunicationHistory"
    - path: "src/services/mock/mock-client.ts"
      provides: "Updated service implementations for folders, versions, comms, client docs, event updates, recurrence, conflicts"
      contains: "getDocumentFolders"
    - path: "app/(tabs)/cases/documents/[caseId].tsx"
      provides: "Folder-organized document list with metadata display and scanning confirmation"
      contains: "folder"
  key_links:
    - from: "src/services/mock/mock-client.ts"
      to: "src/services/types.ts"
      via: "implements updated interfaces"
      pattern: "IDocumentService|ICalendarEventService|IClientService"
    - from: "app/(tabs)/cases/documents/[caseId].tsx"
      to: "src/services/mock/mock-client.ts"
      via: "service calls for folders and documents"
      pattern: "services\\.documents\\.getDocumentFolders"
---

<objective>
Add all Phase 6 types, mock data, i18n translations, and service interfaces. Build the enhanced document screens with folder organization, metadata display, version history, and document scanning confirmation.

Purpose: Establish the data foundation for all Phase 6 features and deliver the document enhancement vertical slice (DOC-01, DOC-02, DOC-03, MOB-01).
Output: Updated types, mock data, i18n, enhanced document list with folders, document detail with metadata/versions, and scanning confirmation flow.
</objective>

<execution_context>
@C:/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/services/types.ts
@src/services/mock/mock-client.ts
@src/services/mock/data/documents.ts
@src/services/mock/data/clients.ts
@src/services/mock/data/calendar-events.ts
@app/(tabs)/cases/documents/[caseId].tsx
@app/(tabs)/cases/documents/preview/[docId].tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 6 types, service interfaces, mock data, and i18n for all domains</name>
  <files>
    src/services/types.ts
    src/services/mock/data/documents.ts
    src/services/mock/data/communication-history.ts
    src/services/mock/data/client-documents.ts
    src/services/mock/data/calendar-events.ts
    src/services/mock/data/clients.ts
    src/services/mock/mock-client.ts
    src/i18n/locales/en/documents.json
    src/i18n/locales/en/clients.json
    src/i18n/locales/en/calendar.json
    src/i18n/locales/sr-Latn/documents.json
    src/i18n/locales/sr-Latn/clients.json
    src/i18n/locales/sr-Latn/calendar.json
    src/i18n/locales/sr-Cyrl/documents.json
    src/i18n/locales/sr-Cyrl/clients.json
    src/i18n/locales/sr-Cyrl/calendar.json
  </files>
  <action>
    Note: This task touches 16 files but the work is mechanical -- 9 are i18n JSON translations (copy structure across 3 locales), 5 are mock data (add fields or create new data files following established patterns), and 2 are type/service definitions. No complex logic involved.

    **Types (src/services/types.ts) -- append after existing types:**

    1. Document enhancements:
       - `DocumentFolder` type: `{ id: string; caseId: string; name: string; icon: string; order: number }`. Predefined folder categories: 'pleadings' | 'evidence' | 'correspondence' | 'contracts' | 'court-decisions' | 'other'.
       - `DOCUMENT_FOLDER_CATEGORIES` const array with those categories.
       - `FOLDER_ICONS` const record mapping each category to an Ionicons icon name.
       - Extend `Document` interface: add optional `folderId?: string`, `tags?: string[]`, `description?: string`, `version?: number`.
       - `DocumentVersion` type: `{ id: string; documentId: string; version: number; size: number; modifiedBy: string; createdAt: string; changes?: string }`.

    2. Calendar enhancements:
       - `RecurrencePattern` type: `{ type: 'daily' | 'weekly' | 'monthly'; interval: number; endDate?: string; daysOfWeek?: number[] }`.
       - Extend `CalendarEvent` interface: add optional `recurrence?: RecurrencePattern`.
       - Add `updateEvent` and `deleteEvent` to `ICalendarEventService`. Add `getConflictingEvents(date: string, startTime?: string, endTime?: string, excludeId?: string): Promise<CalendarEvent[]>`.

    3. Client enhancements:
       - `CommunicationEntry` type: `{ id: string; clientId: string; type: 'call' | 'meeting' | 'email' | 'note'; subject: string; content?: string; date: string; createdAt: string }`.
       - `ClientDocument` type: `{ id: string; clientId: string; type: 'id-card' | 'passport' | 'power-of-attorney' | 'engagement-letter' | 'other'; name: string; uri: string; createdAt: string; expiresAt?: string }`.
       - `CLIENT_DOC_TYPES` const array.
       - `IntakeStep` type: `'contact' | 'conflict-check' | 'consultation' | 'onboarding'`.
       - Add to IClientService: nothing new needed (createClient already exists).
       - New `ICommunicationService`: `getByClientId(clientId: string): Promise<CommunicationEntry[]>; create(data: Omit<CommunicationEntry, 'id' | 'createdAt'>): Promise<CommunicationEntry>`.
       - New `IClientDocumentService`: `getByClientId(clientId: string): Promise<ClientDocument[]>; create(data: Omit<ClientDocument, 'id' | 'createdAt'>): Promise<ClientDocument>; delete(id: string): Promise<boolean>`.
       - Add optional `contacts?: Array<{ name: string; role: string; phone?: string; email?: string; isPrimary?: boolean }>` to `Client` type (for CLNT-04 corporate contacts -- reuse existing `representatives` field by adding `isPrimary` to `Representative` interface).
       - Update `IDocumentService` to add: `getDocumentFolders(caseId: string): Promise<DocumentFolder[]>`, `getDocumentVersions(documentId: string): Promise<DocumentVersion[]>`, `updateDocument(id: string, data: Partial<Document>): Promise<Document | null>`.
       - Update `ServiceRegistry` to include `communications: ICommunicationService; clientDocuments: IClientDocumentService`.

    **Mock data:**

    - `documents.ts`: Add `folderId`, `tags`, `description`, `version` fields to existing mock documents. Assign folders like: doc1 (pleadings), doc2 (court-decisions), doc3 (evidence), doc4 (pleadings), etc.
    - New `communication-history.ts`: Create 8-10 entries across clients c1, c2, c3, c5. Mix of call, meeting, email, note types. Serbian content. Dates spread across recent months.
    - New `client-documents.ts`: Create 5-6 entries across clients c1, c3, c5. Types: id-card, power-of-attorney, engagement-letter. Serbian names.
    - `calendar-events.ts`: Add `recurrence` field to 2-3 existing events (e.g., evt12 team meeting as weekly, evt10 lunch as monthly).
    - `clients.ts`: Add `isPrimary: true` to first representative in each corporate client's representatives array.

    **Mock services (mock-client.ts):**

    - Add mutable copies for communicationHistory and clientDocuments arrays.
    - Add `getDocumentFolders(caseId)`: return predefined folders for given caseId (generate from DOCUMENT_FOLDER_CATEGORIES, each with an id like `fldr-{caseId}-{category}`).
    - Add `getDocumentVersions(documentId)`: return 2-3 mock version entries for the given document (generate inline based on document's createdAt).
    - Add `updateDocument(id, data)`: findIndex/splice pattern to merge partial updates.
    - Add `updateEvent(id, data)`: findIndex pattern, merge partial CalendarEvent, return updated.
    - Add `deleteEvent(id)`: findIndex/splice pattern, return boolean.
    - Add `getConflictingEvents(date, startTime, endTime, excludeId)`: filter calendarEvents by same date, overlapping time ranges (or all-day), excluding the given event id.
    - Implement `mockCommunicationService` with getByClientId (filter + sort by date desc) and create (push pattern).
    - Implement `mockClientDocumentService` with getByClientId, create, delete.
    - Register both new services in the exported `mockServices` object.

    **i18n translations -- add keys to existing JSON files (all 3 locales: en, sr-Latn, sr-Cyrl):**

    Documents: folder names (pleadings, evidence, correspondence, contracts, court-decisions, other), metadata labels (tags, description, version, versionHistory, noVersions, addedBy, changes), folder filter labels, scanning labels (enhancing, usePhoto, retake, scanConfirmTitle, scanConfirmMessage).

    Clients: intake steps (contact, conflictCheck, consultation, onboarding), intake labels (nextStep, previousStep, startIntake, completeIntake), communication history labels (communicationHistory, noCommunications, addCommunication, call, meeting, email, note, subject, content), client documents labels (clientDocuments, noClientDocuments, addDocument, idCard, passport, powerOfAttorney, engagementLetter, expiresAt), corporate contacts (contacts, addContact, primaryContact).

    Calendar: recurrence labels (recurrence, noRecurrence, daily, weekly, monthly, interval, endDate, daysOfWeek), conflict labels (conflict, conflictWarning, conflictingEvents, proceedAnyway), edit labels (editEvent, deleteEvent, confirmDelete, deleteConfirmMessage, save, eventUpdated).
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Check that mock-client.ts exports all new services in the ServiceRegistry. Verify i18n files are valid JSON.</verify>
  <done>All Phase 6 types, interfaces, mock data, mock services, and i18n translations are in place. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build folder-organized document list, document detail with metadata/versions, and document scanning confirmation</name>
  <files>
    app/(tabs)/cases/documents/[caseId].tsx
    app/(tabs)/cases/documents/preview/[docId].tsx
  </files>
  <action>
    **Document list screen (app/(tabs)/cases/documents/[caseId].tsx) -- enhance existing screen:**

    Replace the flat document list with a folder-based view:
    1. Load folders via `services.documents.getDocumentFolders(caseId)` alongside documents.
    2. Add a folder selector at the top: horizontal scrollable row of folder chips (similar to existing filter chips pattern). Each chip shows folder icon + name. Include an "All" option. Use FOLDER_ICONS for the icon.
    3. When a folder is selected, filter documents by `folderId`. "All" shows everything.
    4. For each document card, add a subtitle row showing: tags as small golden pills (if any), and a truncated description (if any). Keep existing file type icon, name, date, size, delete button.
    5. When adding a document (capture photo or upload), show a Modal picker to choose which folder to assign the document to. Default to 'other'. Pass `folderId` when calling `addDocument`.
    6. Keep the existing bottom action bar (capture photo + upload file).
    7. Use SECTION_CARD style for the folder row container.

    MOB-01 - Document scanning confirmation:
    8. Enhance the existing "Capture Photo" flow (which already uses `expo-image-picker` camera). After a photo is captured successfully, show an Alert with options "Use Photo" / "Retake" before saving. This simulates the auto-crop/enhance step (actual image processing requires native modules not compatible with Expo Go, so we simulate with a confirmation step).
    9. When user selects "Use Photo", show a brief "Enhancing..." ActivityIndicator overlay (1-2 seconds via setTimeout) before proceeding to the folder selection modal and saving.
    10. When user selects "Retake", re-launch the camera picker.

    **Document detail/preview screen (app/(tabs)/cases/documents/preview/[docId].tsx) -- enhance existing screen:**

    Add below the existing preview content:
    1. A "Metadata" section card showing: document tags (editable -- tap to add from a predefined list similar to case tags), description (editable text input), and file size/type/date.
    2. A "Version History" section card: load versions via `services.documents.getDocumentVersions(doc.id)`. Display as a vertical timeline list showing version number, date, modified by, changes description, and file size. If no versions, show empty state text.
    3. Use SECTION_CARD style constant for both section cards.
    4. Use the EditableInfoRow pattern from Phase 5 for description field if desired, or a simpler TextInput approach.

    Follow established patterns: inline styles, IoniconsName casting, colors from tokens, useTranslation, useServices hook.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no type errors. Navigate to a case's document list and verify folders display as chips, documents group by folder, metadata pills show on cards. Test capture photo to verify confirmation Alert appears with "Use Photo" / "Retake" options and "Enhancing..." indicator displays. Open a document detail and verify metadata section and version history timeline render.</verify>
  <done>DOC-01: Documents organize into categorized folders. DOC-02: Documents show tags, description metadata. DOC-03: Document detail shows version history timeline. MOB-01: Document capture includes confirmation prompt and enhancement simulation before saving.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Document list shows folder chips and groups documents by folder
- Document detail shows metadata (tags, description) and version history
- Document capture photo shows confirmation Alert and "Enhancing..." indicator
- All 3 i18n locales have consistent keys for documents, clients, calendar
- Mock services implement all new interface methods
</verification>

<success_criteria>
1. All Phase 6 types, service interfaces, mock data, and i18n are in place for documents, clients, calendar, and mobile domains
2. Document list screen shows folder-based organization with folder chips (DOC-01)
3. Document cards display tags and description metadata (DOC-02)
4. Document detail screen shows version history timeline (DOC-03)
5. Document capture includes confirmation and enhancement simulation (MOB-01)
6. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-enhanced-documents-clients-calendar-and-mobile/06-01-SUMMARY.md`
</output>
